{
    "buildCommand": "turbo run build --filter=docs-web",
    "outputDirectory": ".next",
    "installCommand": "pnpm install",
    "framework": "nextjs",
    "devCommand": "turbo run dev --filter=docs-web"
}