# Deprecated Demo

This `demo/` tree is a legacy exploratory app. It is not the recommended Wexts production structure and is not part of the patch-release proof.

Use [`../examples/hello-rpc`](../examples/hello-rpc) as the canonical verified path. The official example is covered by generate, build, start, `/health`, RPC smoke, `doctor`, and `doctor --security` checks.

The files in this directory are retained only for historical reference while older app/template code is consolidated.
